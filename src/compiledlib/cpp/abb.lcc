
typedef ints = native FHEInteger;

ints ints.add(ints x) = native add;
ints ints.times(ints x) = native multiply;

void f(ints[public 100][public 100] a, ints[public 100][public 100] b) {
   for(public int i = 0; i < 100; i = i + 1) 
   {
      for(public int j = 0; j < 100; j = j + 1) 
      {
//         ints res = a[i][0].times(b[0][j]);
         for(public int k = 1; k < 100; k = k + 1) 
         {
          }
      }
   }
}
